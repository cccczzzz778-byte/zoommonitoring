{
  "name": "zoom-monitor-full",
  "version": "2.1.0",
  "private": true,
  "description": "Zero-dependency Zoom participant monitoring dashboard with Telegram alerts and Excel reports",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check main.js"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module"
}
